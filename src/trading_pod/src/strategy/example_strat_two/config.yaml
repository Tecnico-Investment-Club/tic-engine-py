# General Pod Settings
poll_interval_seconds: 60
lookback: 50
assets:
  - AAPL
  - TSLA
  - SPY
  - BTCUSD
  - ETHUSD
  - SOLUSD

# Strategy Configuration
strategy:
  name: "ExampleStrategyTwo"
  timeframe: "1h"
  # trade_every: "1d"  # optional: trade daily using hourly candles
  params:
    rsi_window: 14
    oversold_threshold: 30.0
    max_weight: 0.33
