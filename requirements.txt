# requirements.txt

# Core & Utilities
pydantic==2.6.*         # Strict data validation and models
python-dotenv==1.0.*    # Manages .env files for local development
PyYAML==6.0.*           # For parsing config.yaml files

# Broker & Data Source SDKs
alpaca-py==0.43.*       # Official Alpaca Trading & Market Data SDK
requests==2.31.*        # For hitting Binance public API and Discord webhooks

# Database & Persistence
psycopg2-binary==2.9.*  # Postgres database driver

# Strategy & Data Science
pandas==2.1.*           # Core data manipulation for strategies
numpy==1.26.*           # Numerical processing, required by pandas

# Scheduling
schedule==1.2.*         # In-process job scheduling for the engine's heartbeat

# Time

